<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Bhoomi Jalasandharan Udyami"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
        <activity android:name=".FieldStatusActivity"></activity>
        <activity android:name=".MainActivity" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
\        <activity android:name=".FieldFiltration" />
        <activity android:name=".FieldFertigation" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".PostGsmAuth" />
        <activity android:name=".GsmAuthenticationActivity"/>
        <activity android:name=".FieldConfiguration" />
        <activity android:name=".SettingsActivity" />
    </application>

</manifest>